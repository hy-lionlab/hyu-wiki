version: "3.2"
services:
  http:
    image: nginx:latest
    ports:
      - 80:80
      - 443:443
    volumes:
      - ./http/mediawiki.conf:/etc/nginx/conf.d/default.conf:ro
      - files:/usr/src
    depends_on:
      - mediawiki
  mediawiki:
    build: .
    depends_on:
      - mariadb
    volumes:
      - ./config:/ct:ro
      - files:/usr/src
  parsoid:
    image: thenets/parsoid:0.10.0
    ports:
      - 8081:8000
    environment:
      - PARSOID_DOMAIN_mediawiki=http://http/api.php
  memcached:
    image: memcached:1-alpine

volumes:
  files:
  database:
